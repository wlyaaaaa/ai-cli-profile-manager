# AI CLI Profile Manager 项目设计与实施归档

归档版本：`0.1.0` 首次公开版本；更新至当前源码
归档日期：2026-08-13
目标平台：Windows 11、PowerShell 7

本文是维护者级产品、架构、决策、端口、更新、手册和实施合同的单一归档。用户操作不以本文为准；用户事实源是：

- 《[AI CLI Profile Manager 使用手册](<../user/AI CLI Profile Manager 使用手册.md>)》
- 《[Codex、Claude Code 与 Open Interpreter CLI 中文手册](<../user/Codex、Claude Code 与 Open Interpreter CLI 中文手册.md>)》

## 1. 来源合并说明

本文合并并取代原有：

- 产品设计与用户旅程。
- Profile、端口、更新和中文手册专项合同。
- 产品决策与动态事实纠正记录。
- 0.1.0 实施计划、测试矩阵和完成定义。

旧路径只保留迁移指针，避免项目规则和历史链接失效。研究输入属于非权威归档，不并入产品事实。

## 2. 产品定义

一句话：让 Windows 用户用一个中文入口，按经过验证的 Profile 启动原生 Codex CLI、Claude Code 或当前官方 Rust Open Interpreter，不再反复手改全局环境变量、配置文件和代理进程。

本产品是启动与运维层，不是新的 AI Agent。聊天、历史、终端交互、权限提示、上下文管理和工具执行继续由原生 CLI 负责。

2026-09-15：既有 `codex-qwen3-8-max-paygo` 保留稳定 Profile ID、北京 Workspace 按量端点与凭据绑定，模型身份从可变 alias 固定为 `qwen3.8-max-0902`。Codex Desktop 桥接继续在每次引擎启动时读取原生官方目录，再合并已配置的 0902 与本地模型；云端 Provider 使用 Codex 官方 command-backed auth 通道按需取得 AICLI 加密副本，不把密钥写进模型目录、基础配置或全局环境变量。Password Center 的 `qwen-default` 是上游凭据管理入口，AICLI Codex Profile 是所需运行副本。

2026-09-16：新增 `codex-glm-5-3` 与 `codex-glm-5-3-flash` 两个 exact 中国区 Responses Profile，精确模型 ID 分别为 `glm-5.3` 与 `glm-5.3-flash`，端点固定 `https://open.bigmodel.cn/api/v1`。两者使用独立 Provider/单模型目录、1M 上下文、最大上下文 90% 自动压缩阈值和 `low/high/max`，不加入 Ollama 本地模型集合。Codex Desktop 继续从原生在线缓存取得 OpenAI 模型后合并这两项；Password Center 的 `zhipu-glm-api` 是规范凭据来源，两个 AICLI Profile 是所需加密运行副本。

目标用户：

1. 已使用 Codex CLI、Claude Code 或 Open Interpreter，但需要切换官方订阅、国内 API 和本地模型的 Windows 用户。
2. 会使用 PowerShell，但不希望记忆 Provider、URL、套餐、模型和代理细节的用户。
3. 需要同一套可靠配置跨项目使用，同时不污染官方登录状态的用户。

核心原则：

- 原生优先：不重做 TUI、PTY、会话历史或协议网关。
- 明确选择：用户知道本次引擎、Provider、模型、凭据来源和数据去向。
- 进程隔离：不永久写全局 Provider 变量，不覆盖用户基础配置。
- 证据优先：能打开 CLI 不等于模型或工具可用。
- 安全失败：端口、身份、摘要、秘密和配置冲突不确定时拒绝危险动作。
- 可退出：`native` 和 `eject` 让用户能理解、迁移和脱离本工具。

## 3. 当前范围

### 3.1 引擎与 Provider

| 引擎 | 当前公开路径 |
|------|--------------|
| Codex | 官方 ChatGPT/OpenAI、Qwen3.7 Max 06-08 / Qwen3.8 Max Workspace Responses、GLM-5.3 / GLM-5.3-Flash 中国区 Responses、DeepSeek Flash（`deepseek-flash`）/ V4 Pro CLI-only Responses、本机 Ollama main/review |
| Claude Code | 官方 Claude、Ollama、ChatGPT 双代理、自定义 Anthropic Messages；内置 DeepSeek 云模板已退役 |
| Open Interpreter | 当前官方 Rust `0.0.21+`：本机 Ollama；内置 DeepSeek 云模板已退役 |

2026-08-14 `0.3.6` 在既有 exact 合同上新增 `codex-qwen3-7-max-paygo`，只允许 `qwen3.7-max-2026-06-08`；`codex-qwen3-8-max-paygo` 继续精确 `qwen3.8-max`，DeepSeek Flash/Pro 与本地 main/review 身份不变。所有公开 Qwen/DeepSeek/本地 Codex Profile 均为 Responses、单候选、`flexible=false`、无 fallback，用户 `max` 表示当前模型最高档。两条云 Qwen requested `max` 透明映射 effective `xhigh`；DeepSeek 与本地 Profile 直接使用原生 `max`。Qwen3.7 的其他 Max/Plus 身份和历史入口继续退役。

2026-08-14 `0.3.7` 修复本地 Responses 模型在 Codex app-server 已完成最终输出后，较早公开 `agentMessage` 仍停留在 `started` 而触发 `codex_appserver.item_unfinished` 的终态兼容问题。兼容条件绑定严格事件形态，不维护 CLI 版本白名单：只有全部未完成项都是早于同轮次后续完成、非空 final 的消息时才闭合；任何非消息项、缺少 final、final 之后的新消息或结构漂移继续失败关闭。该合同锁定 0.145、0.147 与模拟高版本，同时不改变 exact Profile、Provider、Responses、MAX、全访问权限和 no-fallback。

2026-08-15 `0.3.8` 新增 `Qwen/Qwen3.8-27B` 的本机 exact Codex/OpenCode 路线。运行时身份固定官方 Ollama `qwen3.8:27b` Q4_K_M，两个 Profile 共用 LocalGpuBroker 公共端点和 262144 原生上下文；Codex 继续固定 Responses、单候选、用户/有效 `max`、受管单模型目录与 no-fallback，OpenCode 固定一次性 pure 配置。官方 1M YaRN 只作为可选扩展事实记录，不在 32GB GPU 上冒充默认稳定合同。

2026-08-15 `0.3.9` 修正“目录 256K、运行时可能按 32K 加载”的证据缺口：两个 Profile 统一改用 `aicli-qwen3.8-27b-256k:2026-08-14`。该运行标签与官方基础标签共用 model/projector/config blobs，只增加 `num_ctx 262144` 参数层；setup 脚本经公共 LocalGpuBroker 创建并回读固定 digest，同时以可恢复备份方式注册 OpenCode Desktop 目录，不改变用户当前选择。

2026-08-15 `0.3.10` 修复 Codex 0.147 app-server 在本地 Responses 路线中对同一个尚未完成 item 重发 `item/started` 时的兼容失败。桥接器只把同 thread/turn、同 item id/type、仍为 `started` 且生命周期投影一致的重发视为幂等；载荷可被补充但不重复计步或计工具。类型/生命周期改变、completed 后重发、未知状态和最终未闭合仍失败关闭，`item/completed` 继续是唯一权威结果。

2026-08-15 `0.3.11` 把 Codex machine run 从一次性 thread 改为可验证恢复的 root-owned run。首次启动使用 `thread/start ephemeral=false`，恢复必须使用 `thread/resume` 并回读原 thread/session、workspace、Profile 指纹、model/provider、requested/effective effort 和全访问权限。状态、事件与 receipt 在 `%LOCALAPPDATA%\AiCliProfileManager\state\recoverable-runs` 按 run/attempt 追加并使用单调 sequence/hash 链绑定；任务正文不持久化。瞬态中断最多自动 exact-resume 3 次，配额暂停不消耗重试次数；无法证明同一 thread 时必须新 workspace 全量重跑，旧 partial 永不合并。

2026-08-15 `0.3.12` 在该恢复控制面上增加显式 `--level agent` 验收，而不另建重试系统。验收只在新的临时目录运行确定性文件任务，要求真实工具活动，并由独立 verifier 校验结果；receipt 绑定 actual model/provider、CLI、五项全访问权限、exact thread/session、恢复次数、工具计数、清理与任务/verifier 哈希。模型自述、普通文本 Live、新 thread、身份漂移或 verifier 失败均不能通过。既有 `all` 保持 text+tool，不隐式触发 Agent 任务。

Open Interpreter 的旧 Python 0.4.x、LiteLLM 旧安装链和旧命令参数不属于支持面。

### 0.3.13 本地主用与启动完整响应修复

- 四个本地 main Profile 统一到 Qwen3.8 27B，显式 27B 入口保持兼容，35B 保留独立复核。
- 使用同权重的新运行标签 `aicli-qwen3.8-27b-256k:2026-09-15`，仅增加 `draft_num_predict 0`，保留 262144 上下文；这是对 Ollama 0.33.1 MTP 初始化崩溃的实测兼容修复。旧标签不覆盖，参数与权重摘要分别验证。
- `doctor <id>` 与 `start <id>` 共用所选本地端点的完整 JSON 检查（连接 2 秒、总计 5 秒）；缺失正文、模型或网关未就绪时返回不可用，不启动上游 CLI。
- 本地 Codex 合并而非覆盖父进程 NO_PROXY，保留已有直连域；菜单显示具体模型版本。
- 默认路线更新须同步 Toolkit 源注册表、运行时镜像与 AICLI 源/安装配置。维护时运行 `.agents` 现有跨入口校验，并传入实际安装模块根验证安装层；每个真实 Profile 指纹单独验收，离线测试使用隔离夹具。

### 3.2 非目标

低频本地使用的换型维护：具体模型仍归现有 `codex-ollama-main.json` / `codex-ollama-review.json` 与对应 catalog，不新增长期模型配置源。准备新模型后，运行 `scripts/Sync-LocalModelProfiles.ps1` 默认预览，`-Apply` 同步其余三个 main 引擎；保持 262144 上下文，各引擎输出/压缩/启动参数继续由各自 Manifest 拥有。固定型号入口保持原样。随后运行 Toolkit 的显式 AICLI 导入、现有安装/诊断与实际使用路线验收，按既有 owner 流程同步派生注册表。静态同步不代表已安装或已通过新模型验收。它是维护辅助命令，不新增常驻调度、自动换型、模型下载或故障回退。

- macOS、Linux 和 Windows 11 以外的平台。
- GUI、全屏 TUI、PTY 外壳、内置聊天窗口或会话数据库。
- 修改或汉化上游 CLI 本体。
- 自研协议转换服务。
- 自动在不同 Provider 或双代理之间故障转移。
- 静默升级 Codex、Claude Code、Open Interpreter、Ollama 或账号相关组件。
- 交互式 `aicli start` 自动放宽上游审批或沙箱。面向程序的 Codex harness 是独立显式合同：所有当前及未来 Codex 模型固定完全访问。
- 未经验证就承诺尚无官方 Responses 合同的未来 Provider；GLM-5.3 与 GLM-5.3-Flash 已按中国区 Codex Responses exact 合同定向纳入。

## 4. 产品对象与数据模型

### 4.1 三层对象

1. **Provider 模板**：随版本发布的只读声明，记录引擎、Provider、套餐、地域、transport、端点、模型、认证类型、能力、来源和兼容性。
2. **用户 Profile**：保存模板 ID、用户自定义 ID/名称、地域、模型、项目偏好和秘密引用，不保存秘密值。
3. **启动计划**：每次 `start` 临时生成，包含可执行文件、工作目录、参数、子进程环境、移除变量、配置文件和代理依赖。

### 4.2 Manifest 基本字段

`schemaVersion`、`id`、`displayName`、`engine`、`provider`、`plan`、`region`、`transport`、`endpoint`、`models`、`auth`、`proxyRef`、`capabilities`、`compatibility`、`verification`、`sources`、`deprecation`。

Manifest 是无执行能力的数据，不允许嵌入 PowerShell。URL、Header、模型和 ID 在进入启动计划前验证；非 localhost 明文 HTTP 默认拒绝。

### 4.3 状态闭集

- `通过`：单个检查项成功。
- `可用`：当前版本要求的真实验证均有证据。
- `可用但有限制`：静态就绪、文本通过但工具未证明，或存在明确限制。
- `不可用`：关键依赖、协议、配置或真实测试失败。

验证记录必须绑定 Profile 指纹、引擎、Provider、transport、端点、模型、CLI 路径/版本、测试层和时间。配置或版本变化后旧证据不能无条件继承。

## 5. 锁定命令面

```text
aicli
aicli setup
aicli version

aicli profile list [--available] [--json]
aicli profile show <id> [--json]
aicli profile configure <template-id> [--id <profile-id>]
aicli profile set-default <id>
aicli profile remove <id> [--yes]

aicli start <id> [--project <path>] [-- <native-args...>]
aicli native <id>
aicli eject <id> [--output <path>]

aicli doctor [id] [--json]
aicli test <id> --live [--level text|tool|agent|all] [--yes] [--json]

aicli proxy <ccp|cliproxy> install
aicli proxy ccp login [codex|device]
aicli proxy cliproxy login [codex|claude|device]
aicli proxy <ccp|cliproxy> logout [--purge-local-auth] [--yes]
aicli proxy <ccp|cliproxy> configure [--port <port>|--auto-port]
aicli proxy <ccp|cliproxy> start|stop|status [--json]
aicli proxy <ccp|cliproxy> update-check|update|native

aicli update check [codex|claude|ollama|interpreter|ccp|cliproxy|self] [--json]
aicli update guide [codex|claude|ollama|interpreter|ccp|cliproxy|self]

aicli help [compact|model|effort|permissions|resume|compare|<command>]
aicli uninstall [--purge-user-data] [--yes]
```

规则：

- 无参数入口是短文本选择器，不是全屏界面。
- 未给 `--project` 时使用当前目录，目录必须存在。
- 只有 `--` 后的字符串逐项传给上游。
- 与启动计划冲突的 Provider、Base URL、认证和产品自管配置入口必须拒绝。
- 未知命令、参数和值缺失立即返回用法错误，不猜用户意图。
- 删除型动作默认确认；`--yes` 只授权该命令列出的本地删除。
- `test` 必须显式 `--live`；非交互需 `--yes`。

退出码：`0` 成功、`2` 用法错误、`3` 可用但有限制、`4` 不可用、`5` 内部错误、`6` 取消。

## 6. 文件、秘密与进程合同

### 6.1 默认布局

| 内容 | 位置 |
|------|------|
| 用户 Profile/设置 | `%APPDATA%\AiCliProfileManager` |
| 秘密、代理、状态、缓存、日志 | `%LOCALAPPDATA%\AiCliProfileManager` |
| CurrentUser PowerShell 模块 | 当前用户 `PSModulePath` 下的版本目录 |
| 命令垫片 | `%LOCALAPPDATA%\aicli\bin` |

所有路径通过 Windows Known Folders 或实际 `PSModulePath` 解析，不在公开模板中硬编码用户目录或盘符。

### 6.2 秘密

- API Key 使用 DPAPI CurrentUser；安全存储失败时不得回退明文。
- 秘密不得出现在 Git、命令参数、PowerShell 历史、日志、JSON、异常、`show`、`native`、`eject` 或测试快照。
- 代理 OAuth 由上游代理写入隔离目录，产品不读取 token 正文。
- 删除最后一个秘密引用的用户 Profile 时，可同时删除本工具保存的密文，但必须在动作前说明。

### 6.3 子进程

- 使用 `ProcessStartInfo.ArgumentList`，不拼接字符串执行，不对用户输入 `Invoke-Expression`。
- Provider 环境只进入目标子进程；父终端保持不变。
- machine child 先清空继承环境，再从 Windows、PowerShell、Node/TLS 所需的小型 allowlist 重建；父环境中的无关凭据、Provider 配置和调试变量不得默认进入子进程。受管计划最后通过 `EnvironmentDelta` 显式注入本次 Profile 必需的 Provider/运行时变量；该显式增量是受信任权限边界。
- `native` 必须展示脱敏后的实际计划；`eject` 只生成无秘密配方。

## 7. 引擎适配合同

### 7.1 Codex

- 公开第三方 transport 只接受 Responses。
- 交互式官方 Profile 使用真实 `CODEX_HOME` 与官方登录；machine run 只复制现有 `auth.json` 到独占 `CODEX_HOME`，不复制配置、规则、技能或其他会话，也不要求付费 API Key。可恢复 run 在终态前持久保留该 home 以支持真实 `thread/resume`；一次性 capture/Live 仍在结束时清理。
- 第三方 Profile 在真实 Codex home 下只写 `aicli-` 命名空间的独立 Profile 文件，并记录受管标记和内容 hash。
- 不覆盖基础 `config.toml` 或未知文件；用户修改后的受管文件不误删。
- 当前 Codex Profile 格式为 `$CODEX_HOME/<name>.config.toml`；旧 `[profiles.*]` 语法不作为当前合同。
- DeepSeek Flash 0731 与 Pro 0813 的官方模型身份分别进入隔离目录，并以内容寻址文件发布到 `aicli-model-catalogs` 受管命名空间；Profile 与 machine override 统一引用同一绝对 `model_catalog_json`。目录只含对应 exact alias，模型版本、Responses 与 max effort 纳入 Profile 指纹。
- Qwen3.8 Max 0902 使用独立 `qwen3.8-max-0902-codex.json`；Qwen3.7 06-08 使用独立 `qwen3.7-max-2026-06-08-codex.json`。两者固定 983616/95%、compact 885254（最大上下文 90%）和用户 `max` → 原生 `xhigh`。Qwen3.8 可变 alias/preview 与其他 Qwen3.7 Max/Plus 的模型、兼容入口和生成分支仍为 tombstone；本地 Qwen 使用各自独立 262144 目录并在 235929（90%）自动压缩。生成器除提示载荷外必须白名单声明能力，禁止照抄空 `base_instructions` 或静默继承其他 Provider 能力后宣称 Agent 行为不变。
- GLM-5.3 与 GLM-5.3-Flash 使用独立 `glm-5.3-codex.json` / `glm-5.3-flash-codex.json`；两者固定中国区 Responses 端点、1048576/95%、943718（最大上下文 90%）自动压缩、单候选和 `low/high/max`。模型能力逐项声明，不继承 Qwen、DeepSeek 或 OpenAI 的能力。两项 GLM 模型通过模型级指令要求根任务和子代理默认使用简体中文；可见思考摘要与进度围绕用户目标解释有意义的发现、原因、影响或取舍，不固定句数、行数或“结论＋下一步”模板。需要调查、工具或多步处理时，首次工具调用前必须主动发送用户可见的助手进度；重要发现、有实质阶段进展、方向变化或影响结果的阻碍也需主动说明，不能仅留在内部分析或最终答复。内部操作转译成用户关心的目的与影响，省略操作细节不等于省略进度本身；无新信息不重复、不编造。曾仅规定表达风格而移除主动触发条件，用户反馈摘要消失；此处恢复明确的发送时机与可见消息要求，保留自然解释风格。最终答复按问题复杂度补足理解所需的背景、依据与细节，省去重复和无关实现过程。这是输出指令，不增加摘要模型调用，也不以配置检查冒充实际表达效果验收。Flash 只向 Codex 暴露当前客户端可传递的文本和图像输入；两项 GLM 模型启用 Codex 客户端的延迟工具搜索，避免在首轮把完整插件工具定义装入上下文。GLM Responses 返回原始 reasoning content 而非 summary 时，桌面桥只在对应 GLM Provider 上把同一内容映射到摘要展示字段，不改写模型回答。
- Claude/OpenCode 的模型窗口放在不可由用户 Profile 覆盖的 `modelMetadata`，纳入 Profile 指纹。非原生 Claude 只为命中元数据的已知最终模型注入 MAX/AUTO，未知模型清除五个上下文控制变量后使用上游保守默认；OpenCode 必须有 context/input/output/reserve/recent/tail 全集，否则启动失败关闭。
- 第三方 API Key 继续由 DPAPI CurrentUser 保存，只通过 Provider 的 `env_key` 对应环境变量进入目标子进程。禁止复制 DeepSeek 官方手工示例中的明文 `experimental_bearer_token`，也不写入 `preferred_auth_method`；AICLI 的隔离合同优先于方便配置的全局示例。
- Codex 思考强度通过当前构建的 `/reasoning`、`/model` 内的 reasoning effort 或配置字段 `model_reasoning_effort` 调整。
- Codex machine run 以当前实装并已验收的 npm `codex-cli 0.147.0` app-server JSON-RPC v2 为协议基线，由受管内部桥保持双向协议；后续更新版本只有通过同一运行时门禁才继续。所有当前及未来 Codex harness 模型在 `thread/start`/`thread/resume` 与 `turn/start` 都选择命名权限 `permissions=:danger-full-access`，并同时绑定 `approvalPolicy=never`；thread receipt 必须实际返回 `activePermissionProfile=:danger-full-access` 与 `sandbox.type=dangerFullAccess`。该不变量按 `engine=codex` 生效，不维护模型 allowlist；显式较低权限、权限身份缺失/漂移、审批或用户输入 RPC 均在模型调用前失败关闭。
- Codex harness 同样按 `engine=codex` 为当前和未来模型默认注册 `thread/start.dynamicTools` 的受管 `public_web_search`；只接受 exact `item/tool/call`、fixed `https://cn.bing.com/search` RSS 和有界参数/响应，拒绝重定向、Cookie、任意 endpoint/Header/凭据。系统代理只为固定 HTTPS 出口服务且不提供默认凭据。`dynamicToolCall` 投影为不含 query/result 的 `web_search` 生命周期；`--no-web-search` 是显式关闭，不得改变权限合同。
- Codex harness 直接运行 npm 包内与当前架构匹配的原生 `codex.exe`，不把短生命周期 Node 启动器作为可清理进程树的根；本地或云端模型都不再降级到外层 read-only/workspace-write 沙箱。其他引擎仍按各自 machine 合同使用外层 Windows 沙箱。任务正文不得写入 argv、环境变量、工作区或持久临时文件。
- Codex machine run 的有效模型必须在启动前从 Profile 默认值与受支持的原生 `--model` / `-m` 覆盖中解析一次，并一致绑定到 Provider `-c model=...`、machine plan、app-server `thread/start` 和公开运行回执。上层请求模型与有效模型不一致，或云端运行缺少有效模型回执时必须失败关闭；不得把请求值直接标成 `actual`。2026-07-28 曾因 bridge 从 Profile `primary` 重建模型而把 Flash/Plus 请求误发至 Max，相关旧 Live 身份证据已撤回。
- Qwen3.7 旧 Agent 记录不能直接恢复使用：只有新 Profile ID 与 exact 06-08 组合被允许，其他模型身份、旧 Profile、目录、导入和 native 参数继续失败关闭；它们不得被重路由到新入口或 Qwen3.8。
- 2026-07-29 的 Spark `workspace-write` 记录只保留为旧协议历史，已被 `0.3.5` 的通用 Codex `danger-full-access` harness 合同取代；权限通过、协议通过和模型能力通过仍必须分别记录。
- `0.3.12` 是当前源码与安装目标。验收记录必须绑定实际入口；在正式安装/候选切换前，不得用 source 结果声明 installed/runtime/live current。
- 观察层只接受 app-server 同一份完整运行时快照中的 `tokenUsage.total`、`tokenUsage.last.totalTokens`、`modelContextWindow` 与真实 `contextCompaction` item。运行累计 input/cache/output/reasoning/上游 total 必须来自 `total`；当前上下文必须来自 `last.totalTokens`。累计字段缺失时不得回退到 `last`，缓存为 `0` 时省略，缓存只作为输入子集展示且不得重复加入总计，推理输出保持独立。首次 run 建立 thread；后续 exact-resume 复用同一 thread，因此 `total` 是该 thread 的累计快照，每个恢复 attempt 的 usage 只能由单调累计快照差分得到；计数器回退必须失败关闭。不以配置上限或字符数估算。公开 `agentMessage` delta 按短语聚合成 `output.delta`；公开进度、粗粒度工具事件和最终结果可见，reasoning 正文不公开。`maxSteps` 采用 `distinct-non-output-thread-item-v2`，只约束不同的非输出行动 item；公开 progress/final message 不消耗行动步骤，但仍受墙钟、输出和事件门约束。0.145 的 `subAgentActivity` 是 completion-only 点事件；其他受支持 item 严格执行 started → completed，并绑定精确 thread/turn，成功轮次必须以 `status=completed` 结束。真实复杂任务证明 0.145 和 0.147 可能为多个公开进度消息省略 completion；兼容层按事件形态只把全部早于同轮次后续、已完成且有公开正文 final 的 `agentMessage` 标记为 superseded，后续保持相同结构的高版本自动继承。无后续 final、final 之后的新 orphan、任何非消息 orphan 或协议结构漂移不得兼容。事件文件不得保存 prompt、reasoning、命令、工具输出或压缩 history；低于基线、必要字段缺失或结构漂移、未知通知、scope/status/lifecycle 不合法、服务端请求或无法确认的 app-server 进程树清理必须失败关闭。

### 7.2 Claude Code

- 直连模板只接受 Anthropic Messages 兼容路径。
- `claude-custom` 要求 HTTPS 或 localhost HTTP、自定义 Key、主模型和可选小模型。
- ChatGPT 订阅只能通过明确标识的第三方 managed-proxy Profile，不伪装直连 Provider。
- Provider 环境只注入子进程，不自动改写用户/项目 `settings.json`。
- 启动前应计算会影响 Provider 的用户、项目、本地和受管设置层；无法安全隔离时阻止启动并指出文件和字段名。
- 权限与 effort 保留 Claude 原生语义；第三方 Provider 能力不得冒充官方模型等价能力。
- Claude Code 不再公开内置 DeepSeek 模板；旧 `claude-deepseek`、V4 Flash Profile 与 2026-07-14 的旧 Pro Live 记录只保留为历史事实，不能证明当前 `codex-deepseek-flash` 或任何新 Profile。Codex 当前以 `codex-deepseek-flash` → `deepseek-flash` 为主入口，V4 Pro 只保留独立 CLI-only Profile。

### 7.3 Open Interpreter Rust

- 只接受当前官方 Rust CLI `0.0.21+`；检测并拒绝旧 Python 0.4.x。
- 千问与 Ollama 使用 Responses wire；Open Interpreter 的内置 DeepSeek 云模板已退役，不再维护 DeepSeek Chat wire 公开入口。
- 旧 `oi-deepseek` / `deepseek-v4-flash` Profile 必须失败关闭；历史 DeepSeek Live 记录不得跨当前模板指纹复用。
- 通过临时 `-c` TOML 覆盖设置 `model_provider`、`model`、`base_url`、`wire_api` 和 `env_key`，不写旧 YAML Profile。
- 云端 Key 只进入 `AICLI_OI_PROVIDER_KEY` 子进程环境，并通过 `shell_environment_policy.exclude` 从 Shell 工具排除。
- 不启用旧版自动执行、命令行明文密钥或任何审批/沙箱绕过参数。
- Live 文本测试使用真实目标 CLI 与临时空目录。Codex 统一走 app-server `danger-full-access`，以观测到零次工具调用作为通过条件并回读权限身份；首个工具事件触发终止，但不冒充执行前工具禁用。其他引擎继续使用各自可证明的工具禁用、配置隔离与较低权限路径。

## 8. ChatGPT 双代理与端口合同

固定上游：

- `ccp`：`raine/claude-code-proxy`，首选端口 `43197`。
- `cliproxy`：`router-for-me/CLIProxyAPI`，首选端口 `43198`。

### 8.1 供应链

- 任一 Windows artifact 必须匹配产品随版本发布的精确 SHA256，或固定可信发布者签名。
- HTTPS、GitHub 来源、下载后临时算摘要或“上游没有 checksum”不能替代批准证据。
- 解压前枚举条目并拒绝绝对路径、`..`、链接逃逸和清单外可执行文件。
- 首次安装在临时目录完成摘要、安全解压和结构验证后才设置 `current`；启动时完成进程身份、健康响应与 loopback 监听验证后才保存运行状态。`0.1.0` 不执行已安装代理的受管版本切换；后续版本若启用更新，必须先完成运行健康验证并支持失败回滚。

### 8.2 端口

- 只允许 IPv4 loopback `127.0.0.1`；`0.0.0.0`、`::`、局域网和公网监听均失败并停止本次受管进程。
- 候选池为 `43192-43209`，两个代理采用不同轮转起点。
- 分配时持有同用户互斥锁，排除 TCP 动态范围、Windows excluded ranges、活动 listener 和真实 bind 失败。
- 不修改动态端口范围、excluded range、防火墙或 URL ACL，不杀未知占用进程。
- 成功启动并验证新进程持有 listener 后才持久化端口。

### 8.3 身份和生命周期

运行状态记录代理 ID、PID、启动时间、可执行路径、版本目录、配置路径、host、port 和 nonce。停止、更新或卸载前重新核验 PID、启动时间、路径和 listener；身份不匹配不终止。

登录只调用上游交互，不读取 OAuth 正文。`logout` 必须分别报告本地清理和远程撤销；强制删除本地认证目录不等于远程撤销。

## 9. Doctor 与 Live Test 合同

### 9.1 Doctor

按稳定 check ID 检查：平台与 PowerShell、CLI/代理/Ollama 版本、安装来源、登录状态、Manifest/Profile/schema/秘密引用、父环境与有效配置层冲突、套餐/地域/端点/模型、代理身份和 listener、Ollama 服务/模型、验证记录有效性。

Doctor 默认不发模型生成请求。没有安全、零费用认证接口时明确写“未执行”，不得用自制 HTTP ping 冒充目标 CLI 已通过。

### 9.2 Live Test

1. 必须显式 `--live`，先展示 Provider、数据去向和额度风险。
2. 创建随机临时空目录，不读取用户项目。
3. 使用真实目标 CLI 的非交互入口。Codex 保持通用 `danger-full-access` 合同，并且只有最终观测到零次工具调用才通过；首个工具事件触发终止，但不是执行前工具禁用。其他引擎隔离私人规则、设置、MCP、Hooks、Skills 和普通工具。
4. 文本层要求进程退出码 0 且最终模型正文严格等于 `PONG`。
5. 工具层只暴露不接受任意路径/命令的 nonce 工具；无法隔离时跳过并报告限制。
6. 保存结构化版本和 Profile 指纹，不保存提示、回复、项目文件或秘密。
7. `finally` 清理临时配置、工具进程和目录；异常也要覆盖旧验证记录。

## 10. 安装、更新、卸载和恢复

### 10.1 安装

- CurrentUser PowerShell 模块，不需要管理员权限，不全局放宽 ExecutionPolicy。
- 从 Manifest 读取版本，复制到临时候选目录并导入验证。
- `-Force` 才允许替换已有同版本；替换失败恢复备份。
- 创建用户 PATH 垫片并尝试加入 PowerShell 自动导入块。
- 用户数据独立于模块版本，重装或上游 CLI 更新后仍可重新生成启动计划。
- Codex Desktop 的内容寻址 bridge 启用采用受保护登记前置门：候选 release 可以先构建到 `%LOCALAPPDATA%\AiCliProfileManager\desktop\releases\<16-hex>`，但在修改 Codex 配置、`CODEX_CLI_PATH` 或 `desktop\state.json` 之前，必须逐文件命中 PCConfig AuthorityHost 已安装的 `aicli_desktop_bridge.json`。未登记、文件数/路径/大小/SHA-256 不一致或 registry 非正规文件时直接失败关闭；AICLI 不自行把新 release 加入信任清单。未来 bridge 更新顺序固定为“构建候选 → PCConfig 登记并安装精确 7 文件清单 → AICLI Enable → 状态回读”，从而避免版本切换先发生、Secret Broker 后失效。
- `0.3.10` 起的安装器对 Qwen3.7 旧用户 Profile、受管 Codex TOML/catalog/state 和旧模块版本做无副作用预检；只隔离 marker/body/state/hash 及 Manifest 身份闭合的旧项，并明确保留新 exact 06-08 Profile/TOML/state/被引用目录。未知、篡改或 reparse 项在任何迁移前失败关闭。
- 退役预检与运行时同样优先既有真实 `CODEX_HOME`。历史 managed state 中的旧路径仅当其直系父目录是单目标 Junction、父链均为普通目录、唯一目标为非空完整绝对路径且严格等于已验证真实 home 时才可作等价比对；旧记录路径只用于 Junction 元数据比对，退役产物的扫描、移动和存在性检查始终使用真实路径。显式传入的 home junction、循环、多层或越界链接仍失败关闭。实际写入前会即时重验固定根和直接子项；验证失败即停止，不把此描述为无竞争保证。
- 退役产物移入 `%LOCALAPPDATA%\AiCliProfileManager\retirement\qwen37-v1` 可恢复 quarantine，并保留 metadata 前像；SecretRef/密钥永不读取、复制、移动或删除。迁移失败尽最大可能回滚已完成的移动和 metadata。

### 10.2 更新

- 先识别实际安装来源，再给同渠道命令。
- 不混用原生安装器、npm、WinGet 等渠道。
- 上游 CLI 只检查和指导，不静默升级。
- Open Interpreter Rust 可使用当前官方 `interpreter update`。
- 受管代理只有批准 artifact 才显式更新并支持回滚。
- 本工具 self update 在有真实 GitHub Release 来源后才启用。

### 10.3 卸载

- 普通卸载停止己方代理、删除模块，保留 Profile、DPAPI 秘密、代理数据和导出物。
- `--purge-user-data` 才删除本工具 AppData/Local 数据。
- 不卸载上游 CLI、官方登录或本地模型。
- 删除代理本地 OAuth 前先执行或引导 logout，并分别展示本地删除与远程撤销状态。
- 用户修改过或身份未知的 Codex 派生文件必须保留并报告。

## 11. 用户旅程

### 11.1 首次使用

1. 说明产品只是启动/运维层。
2. 检测 Windows、PowerShell 和目标 CLI。
3. 选择引擎、Provider、套餐、地域和模型。
4. 显示数据去向，无回显录入必要 Key。
5. 运行零费用 Doctor。
6. 用户自行决定是否执行可能耗额度的 Live Test。
7. 在当前目录启动或仅保存 Profile。

首次引导不能要求用户先理解 TOML、环境变量或代理原理。

### 11.2 日常使用

- 显式 `start <id>` 优先，默认/最近 Profile 只用于减少输入，不隐藏选择。
- 项目目录来自当前 shell 或显式 `--project`。
- 切换 Provider 启动新进程；不会在已有会话中热切换。
- `profile show` 看数据去向，`native` 看启动计划，`doctor` 看静态问题，`test --live` 才证明真实链路。

### 11.3 反废案

- `native` 展示脱敏真实配置和命令。
- `eject` 导出无秘密独立配方。
- 代理型导出记录精确仓库、版本、asset、SHA256、安装和重新登录说明，不复制 OAuth 或捆绑未知二进制。

## 12. 中文手册合同

每个功能先写：

1. 功能干什么。
2. 可复制命令。
3. 执行后会怎样。
4. 何时生效。
5. 1–3 个常用例子。
6. 出错先做什么。
7. 最后才放协议、文件和边界。

风险必须在动作前说明：额度、联网、写入、删除、代理启停、Provider 数据去向、远程撤销与本地删除、新进程生效。

解释中文优先，命令和上游错误保留英文。不要虚构参数，不把“打开成功”写成 Provider 可用，不混淆下一请求、新会话和新进程。

用户文档只维护两本 canonical Markdown；学习篇放在 CLI 手册最后。旧页面仅作迁移指针，不复制正文。PDF 从同版本 Markdown 生成，并单独做视觉验收。

## 13. 实施阶段归档

### 阶段 0：动态事实

核对 Codex/Claude/OI 当前 CLI、Profile 格式、Provider 端点、模型、代理版本与 Windows 行为。

### 阶段 1：模块与路由

实现单一入口、严格命令语法、统一退出码、UTF-8 输出、CurrentUser 安装与开发启动器。

### 阶段 2：Manifest 与 Profile

实现 schema、模板加载、多实例用户 Profile、原子 JSON、状态词汇和 Profile 指纹。

### 阶段 3：秘密与启动计划

实现 DPAPI、无回显输入、进程环境隔离、参数数组、`native`、`eject` 和端点/ID/模型校验。

### 阶段 4–5：Codex 与 Claude

按官方、第三方云 API、本地 Ollama、custom 顺序完成 adapter；验证官方登录不被第三方配置劫持。

### 阶段 6：双代理

实现批准 artifact、安全解压、端口互斥与重试、loopback、进程身份、登录/退出、更新回滚和两个 Claude Profile。

### 阶段 7：Doctor 与 Live Test

实现分层 check、真实 CLI 文本探针、工具隔离、版本/指纹记录和异常清理。

### 阶段 8：帮助、更新、安装和恢复

实现命令帮助、来源检测、原子安装、PATH 垫片、卸载、恢复和 self update 边界。

### 阶段 9：中文手册

完成两本 canonical Markdown、索引、迁移指针、法律和兼容性状态页。

### 阶段 10：公开候选

完成离线测试、打包、SHA256、第三方清单、秘密/路径扫描、干净账户安装/重装/升级/卸载和真实 Provider 矩阵。公开远端、PR 和 Release 仍需要单独授权。

## 14. 自动测试与真实验收

自动测试至少覆盖：

- Router：语法、未知参数、`--`、退出码、纯净 JSON。
- Paths/JSON/Secrets：Known Folders、中文/空格路径、原子写入、DPAPI、脱敏。
- Manifest/Profile：schema、套餐、状态、删除和秘密引用。
- Codex/Claude/OI adapter：配置优先级、保留参数、秘密不进 argv、Rust/旧 Python 识别。
- Child process：环境注入/清理、超时、取消和大输出。
- Proxy/ports：动态和排除范围、IPv4/IPv6 listener、并发、PID 复用、摘要、解压和回滚。
- Doctor/Live：无网络静态检查、严格 PONG、工具跳过、过期证据、临时目录清理。
- Docs/Release：两本手册、索引顺序、本地链接、动态来源、无旧 OI 接口、三套思考命令不混用、安装/卸载和包内容。

真实验收使用有权访问的账号/Key，在临时空目录执行；CI 不保存真实秘密、不跑付费请求。每条路径分别记录文本、工具、版本、模型、端点和限制。

## 15. 决策史

### D-001 至 D-009：产品形态

- D-001：建立独立公开项目与独立品牌 `AI CLI Profile Manager` / `aicli`。
- D-002：保留原生 CLI，不做 GUI、PTY 或会话外壳。
- D-003：Codex 首版只做官方、Ollama 和千问 Responses；该历史范围在 DeepSeek 官方于 2026-07-31 开放 Codex Responses 后由 D-029 定向扩展，不再作为禁止 DeepSeek 的现行结论。
- D-004：Claude 首版覆盖官方、DeepSeek、千问、Ollama、双代理和 Anthropic custom。
- D-005：双代理固定为 `raine/claude-code-proxy` 与 `router-for-me/CLIProxyAPI`。
- D-006：不汉化 CLI 本体；中文手册保留原生命令。Codex 思考入口按当前构建记录为 `/reasoning`，并保留 `/model` 内 reasoning effort 的兼容说明；Claude 保持自己的 effort 命令。
- D-007：Provider Manifest 允许后续扩展，但不预先承诺新厂商。
- D-008：公开状态必须有当前证据。
- D-009：首个版本从 `0.1.0` 开始，稳定后再讨论 `1.0.0`。

### D-010 至 D-018：安全与运维

- D-010：不自动故障转移或静默升级。
- D-011：交互式 `aicli start` 默认不绕过原生权限。该历史决定不适用于 D-033 明确规定的 Codex harness。
- D-012：上游更新只检查和指导。
- D-013：代理端口动态验证、成功后持久化。
- D-014：中文手册先讲用途、命令和效果。
- D-015：项目代码使用 MIT License；第三方许可证独立。
- D-016：秘密使用 DPAPI CurrentUser。
- D-017：CurrentUser PowerShell 模块安装，不要求管理员。
- D-018：首版命令面锁定，品牌和版本应集中定义。

### D-019 至 D-027：验证与实施修正

- D-019：Ollama 不设跨硬件万能推荐模型；公共模板只使用标准端口和公开模型。
- D-020：未知代理二进制绝不下载执行。
- D-021：工具自检只暴露单用途 nonce 工具。
- D-022：Codex 复用真实 home，只写可验证身份的派生 Profile。
- D-023：本地 OAuth 清理不冒充远程撤销。
- D-024：无法证明工具隔离时诚实跳过并降为有限制。
- D-025：代理批准清单可以为空；无批准证据时拒绝安装。
- D-026：仓库身份、机器配置与产品运行态由各自事实源管理，不能把机器私有事实写进公开产品文档。
- D-027：统一 JSON 外壳、开发入口、代理型 eject 锁文件、离线 Smoke/CI 和兼容性单页作为实施增强。

### D-028：Open Interpreter 世代切换

首版只支持当前官方 Rust Open Interpreter `0.0.21+`，拒绝旧 Python 0.4.x。Provider 采用 Rust CLI 的 TOML 配置与环境密钥引用，不能把旧参数兼容层继续作为公开能力。

### D-029：DeepSeek Codex Flash/Pro exact 接入

`0.3.4` 将 Codex 的 `deepseek-v4-flash` / `DeepSeek-V4-Flash-0731` 与 `deepseek-v4-pro` / `DeepSeek-V4-Pro-0813` 分成两个 Responses exact Profile，均固定 1M 与用户默认 `max`。Claude Code 走 Anthropic、Rust OI 走 Chat，并继续保持 Flash-only。Qwen Code 0.21 与 OpenCode 1.18.8 即使有上游原生支持，也不能绕过 AICLI 当前禁网 machine 沙箱：在真实 Key 可能进入 Shell 且缺少受控远程 egress relay 时，不创建假 Profile。

### D-030：第三方上下文连续性以原生 ChatGPT + Codex 为基准

原生 `codex-official` 不附加目录或压缩策略。第三方 Codex 无 OpenAI remote compact 时使用受管模型目录修正真实窗口，云千问与本地 Qwen 分离；Claude Code 只为已知模型设置官方支持的窗口变量，不设置提前压缩百分比，也不禁用溢出保护，未知模型会清除继承控制项；OpenCode 的一次性配置对 `qwen-main-v1` 显式声明 262144/8192，对固定 Qwen3.8-27B 256K 运行标签声明 262144/32768，并共用 20000 reserve、4 轮/16384 token 保留与 `prune=false`。这些设置只能延后并减少不必要的有损摘要，不能把摘要宣称为无损。

AI 使用合同：一个会话/run 对应一个内聚里程碑；AICLI 的 `aicli.third-party-continuity.v1` 契约要求主动 compact 前先把目标与验收、约束/授权/owner、规则与关键文件、改动及脏改动归属、决定、测试/Live 缺口、阻塞与下一步持久化到项目已有状态，压缩后把摘要当线索，重读项目规则、状态文档与工作树（`git status`/`git diff`），不建立第二事实源。难题与最终判断不得为节省 token 下放给能力未通过的模型。远程 Qwen/DeepSeek OpenCode 仍受 key-isolated egress relay 门禁，与压缩修复相互独立。

### D-031：PDF 构建必须有界并可视觉复核

两本 canonical Markdown 的 PDF 构建优先使用仓库随附 helper 调用本机已有 Playwright 与 Edge，隔离于日常浏览器进程；缺少该运行时时才进入有界超时的 Edge CLI 后备。生成物必须记录源 Markdown SHA256，完成文本抽取、全页渲染和代表性密集页检查后才能随文档提交，不能把“进程退出码为零”单独视作版面通过。

### D-032：Qwen3.7 Max/Plus 永久退役

Qwen3.7 Max/Plus 全家族不再是候选或兼容层：移除 Codex、Claude Code、Open Interpreter Manifest、目录、生成器分支和 OpenClaw 导入入口；旧用户 Profile、最终启动计划以及 native model/fallback 参数都必须命中统一 tombstone 并失败关闭。退役身份不能自动映射到 Qwen3.8，也不能通过旧安装 payload 或遗留受管 Codex 文件继续启动。

升级安装必须先作全量预检，再把身份闭合的退役入口移入可恢复 quarantine；任何未知或用户改写项阻断本次安装且不被删除。用户 SecretRef 保留，后续只能在身份相同的凭据域内显式复用，不进行明文迁移。

### D-033：所有 Codex harness 当前及未来模型一律完全访问

`aicli run` 的 Codex 路由按引擎而不是模型列表固定原生 `danger-full-access` 与 `approvalPolicy=never`。每个新模型、Provider 或本地 Profile 自动继承同一权限合同；请求较低权限不是兼容模式，而是调用前错误。桥接器必须从 app-server 回读 `dangerFullAccess`，同时回读 exact model/modelProvider 并拒绝 reroute。交互式 `aicli start` 仍由用户和原生 CLI 管理权限，不与 harness 合同混淆。

Codex 文本 Live 也必须走该通用 harness，并保存 exact runtime executable/version 与 `approval_policy`、`requested_policy`、`sandbox_boundary`、`sandbox_type`、`permission_profile` 五项运行时证据。本次 Live 额外要求观测到零次工具调用；首个工具事件触发终止和失败，但该机制不是执行前禁用、沙箱或降权。

### D-034：窄恢复 Qwen3.7 Max 06-08 exact Codex Profile

用户 2026-08-14 的新明确指令窄范围取代 D-032 的“全家族永久退役”：只新增 `codex-qwen3-7-max-paygo` → `qwen3.7-max-2026-06-08`。该入口固定北京 Workspace paygo Responses、单候选、无 fallback、983616/95%、compact 885254（最大上下文 90%），并把用户 `max` 映射原生 `xhigh`。同一 Workspace endpoint 才可显式复用 Qwen3.8 的不透明 SecretRef。

D-032 对通用 `qwen3.7-max`、05-20、preview、Plus、旧 Profile ID、Claude/OI/OpenClaw route、native model/fallback 和旧安装入口继续有效。06-08 仍在全局 tombstone matcher 中，以阻止自由模型覆盖；只有同时命中新 exact template/profile identity 的上下文门才允许。迁移同样必须按身份保留新 exact artifacts，而不是把“出现 06-08 字符串”直接当作旧入口。

### D-035：exact Codex Profile 使用快速启用与正式发布双车道

维护者从 clean commit 本机启用一个 exact Codex Profile 时，使用 `Install-ExactCodexProfileFast.ps1`。快速车道必须闭合 Manifest/目录单模型身份、Responses、`flexible=false`、用户 `max`、无 reserved/fallback、退役上下文门、one-command、SecretRef 与安全回归，再执行发行 smoke、原子安装和固定 shim 的 `list/show` 回读；它不得构建 PDF/ZIP、执行付费 Live 或把 `liveAcceptance=not-run` 冒充当前 Live。

正式公开发布仍需全量 Pester、canonical 文档/PDF、发行 ZIP/hash、公开暴露检查、Git 收口以及按授权执行的独立 Live。两条车道共享同一 source of truth；快速车道缩短本机可用等待时间，不降低 exact/no-fallback/secret/launch-plan 门禁。`--reuse-secret-from` 命中同一 Qwen Workspace 凭据域时同时盲复用规范化 endpoint，避免重复输入或回显 Workspace 标识。

## 16. 动态事实纠正记录

- Codex 当前 Profile 是独立 `<name>.config.toml` 文件，不使用旧 `[profiles.*]` 嵌套方案。
- Codex 第三方 Provider 只接受 Responses wire；Chat Completions 不能只改 Base URL 强行接入。
- GLM 中国区 Codex 只接受 `https://open.bigmodel.cn/api/v1` Responses；`glm-5.3` 与 `glm-5.3-flash` 分别使用 exact Profile，禁止把 Coding Plan Chat Completion 地址写成 Responses。
- DeepSeek Codex 当前主入口使用官方自动升级模型 ID `deepseek-flash`，AICLI Profile 为 `codex-deepseek-flash`；`deepseek-v4-pro` 仅通过 `codex-deepseek-v4-pro` CLI-only Profile 保留。旧 `codex-deepseek` / `deepseek-v4-flash` 与其他 V4 Flash 身份失败关闭。官方示例允许的全局明文 Key 写法不属于 AICLI 安全合同。
- 千问按量、Token Plan 和 Coding Plan 的 Key、端点与能力必须分开。
- 双代理是第三方本地程序，不是 OpenAI/Anthropic 官方能力。
- Codex reasoning effort 通过当前构建的 `/reasoning`、`/model` 内选择或配置字段调整。
- Open Interpreter 当前官方实现为 Rust；旧 Python 文档、参数和本机验证记录全部失效，必须重新验收。
- 讨论归档或截断输入不能直接成为实现真相。

## 17. 官方与上游参考

### Codex / OpenAI

- [Profiles](https://learn.chatgpt.com/docs/config-file/config-advanced#profiles)
- [Custom model providers](https://learn.chatgpt.com/docs/config-file/config-advanced#custom-model-providers)
- [Configuration schema](https://learn.chatgpt.com/docs/config-schema.json)
- [Slash commands](https://learn.chatgpt.com/docs/developer-commands)

### Claude Code / Anthropic

- [Commands](https://code.claude.com/docs/en/commands)
- [Environment variables](https://code.claude.com/docs/en/env-vars)
- [Model and effort](https://code.claude.com/docs/en/model-config)
- [Permissions](https://code.claude.com/docs/en/permissions)
- [Installation](https://code.claude.com/docs/en/installation)

### Open Interpreter

- [Repository](https://github.com/openinterpreter/openinterpreter)
- [Install](https://www.openinterpreter.com/docs/terminal/install)
- [CLI Reference](https://www.openinterpreter.com/docs/terminal/cli-reference)
- [Config](https://www.openinterpreter.com/docs/terminal/config)
- [Providers](https://www.openinterpreter.com/docs/terminal/providers)

### Provider 与代理

- [千问接入 Codex](https://help.aliyun.com/zh/model-studio/codex)
- [千问接入 Claude Code](https://help.aliyun.com/zh/model-studio/claude-code)
- [DeepSeek Codex integration](https://api-docs.deepseek.com/quick_start/agent_integrations/codex/)
- [DeepSeek Responses API](https://api-docs.deepseek.com/guides/responses_api/)
- [DeepSeek Claude Code integration](https://api-docs.deepseek.com/quick_start/agent_integrations/claude_code/)
- [DeepSeek Change Log](https://api-docs.deepseek.com/updates)
- [Ollama Codex](https://docs.ollama.com/integrations/codex)
- [Ollama Claude Code](https://docs.ollama.com/integrations/claude-code)
- [raine/claude-code-proxy](https://github.com/raine/claude-code-proxy)
- [router-for-me/CLIProxyAPI](https://github.com/router-for-me/CLIProxyAPI)

## 18. 公开发布完成定义

发布前必须同时满足：

1. 干净 Windows 11 标准用户可安装、重装、升级、卸载和恢复。
2. 所有公开命令、未知参数、中文/空格路径和退出码自动测试通过。
3. 两本 canonical 手册与实现一致；旧页面仅作迁移入口。
4. 三套思考命令不混用，OI 文档无旧 Python 安装步骤或旧接口。
5. Release ZIP 不含机器私有路径、研究输入、原始日志、秘密或维护者规则。
6. ZIP、SHA256、SBOM/第三方版本清单和许可证齐全。
7. 当前 Provider 矩阵有真实目标 CLI 的版本、模型、端点、指纹和结果；未测路径明确标待验收。
8. 双代理仍为可选第三方通道；未完成 OAuth/Live 时不标可用。
9. GitHub Security 私密报告入口、隐私、贡献、第三方声明和 Changelog 可用。
10. 公开远端、push、PR 和 Release 只在获得明确授权后执行。

## 19. Codex 桌面原生本地模型接入

2026-09-15 用户明确要求现有桌面版显示具体型号并可选 Qwen3.8 27B、Qwen3.6 35B，保留 OpenAI 动态模型发现。本功能属于 AICLI 的配置和原生启动适配，不创建 GUI、Agent、聊天库或模型 API 网关；既有 `aicli run` machine harness 的事件闭集不套用于桌面完整协议的透明转发。

- 源码归 `src/AiCliProfileManager/Support/DesktopBridge`，本地配置发现归同级 `GetDesktopModelPlan.ps1`，构建/启停归 `scripts/Set-CodexDesktopLocalModels.ps1`。用户说明见《Codex 桌面本地模型》。独立适配器用 .NET 10，未改变现有 PowerShell 模块的导出命令面。
- 官方应用的 `CODEX_CLI_PATH` 只替换启动入口，原版 Codex 引擎仍唯一执行模型请求、工具和历史管理。适配器不读写官方 `auth.json`，不代理 OpenAI HTTP，不修改 app.asar、CLI 本体或全局 `model_catalog_json`。
- 每次启动前，先调用未覆盖目录的原生 `debug models` 获取当前官方完整元数据，失败时使用原生 bundled 目录；合并当前 AICLI 本地元数据后，以进程级 `-c model_catalog_json=...` 启动 app-server。模型 ID 冲突不替换官方模型。`model/list`、分页和 `config/read` 全部原样透传，不再制造显示专用配置。目录是启动快照，下一次启动动态刷新；用户全局 config 不写静态模型清单。
- 原生持久任务保留最初 provider。隔离 mock SSE 回合实验确认：即使设置 `thread_unload_delay_secs=0`、回读已卸载，再用同 ID resume，新 provider 也不覆盖原 provider。因此新任务选择官方/本地服务；本地两个模型共用 `aicli_desktop_local`，可以在同一本地任务切换。跨服务的旧任务选择明确报错，不复制历史、新建替身或静默换服务。
- 既有本地 CLI provider 和旧运行标签按实际配置兼容。旧标签保留真实请求标识并隐藏于一般新任务列表；当前使用旧标签时显示具体型号与“旧配置”，不自动换权重或运行参数。本地 Responses 路径按 Profile 的 `auth.type=none` 使用公共网关；machine-run 专属 GPU session 不被误加为交互式桌面前置。
- 安装采用内容版本目录，记录原用户级 `CODEX_CLI_PATH` 并提供恢复；当前 AppX 引擎按内容哈希匹配缓存，缺失时从已注册安装包补齐引擎及伴随程序，覆盖桌面更新但旧缓存仍存在的情形；不会关闭正在运行的桌面任务。新的桌面进程才能加载入口。构建、透明协议回归、两个本地模型真实调用、安装回读与前台重新打开后的显示/选择分别验收。

验证分层：前台已由用户确认两个型号可见、可选；简单 PONG 曾通过，但未能发现工作流回退问题，因此不作为完整验收。已定位并修正启动目录注入层级：相同桌面配置下，旧版展开约 860110 字符工具定义，启动级完整目录恢复后约 31741 字符；真实 35B 同环境请求 input=14728，并产生 exec_command 调用。隔离实跑中两个模型都调用工具读取了随机文件内容（35B附带格式化说明，内容匹配）。基础指令恢复至本地目录的 17730 字符/53次 skill 词出现。无真实档案问题重放；统计对照不保存请求正文。透明传输6项、路由12项、桌面更新解析3项通过。窗口仍为本地真实262144，未通过扩大容量或删除技能/MCP掩盖问题。

最终验收（2026-09-15）：修复版已安装后，用户明确确认重启 E2E（端到端）验收通过，并要求结束本次项目工作。此结论来自用户实际桌面使用验收，补齐此前仅有显示/选择与引擎测试的证据；本次原生桌面本地模型接入交付完成，无待验收项。按用户要求不重复运行验收、不再次安装或重启。后续桌面版本的兼容性沿上述动态模型目录与当前安装包引擎解析维护，不将本次通过表述为对所有未来版本的保证。

## 20. 本地模型集合统一维护

2026-09-15 用户扩展为四模型统一入口。模型事实仍归既有 Provider Manifest，data/local-model-set.json 只选择启用集合及稳定客户端 ID 映射。scripts/Sync-LocalModelConfiguration.ps1 同步安装、Desktop、OpenCode、Toolkit 与本机镜像，并核对 Ollama 实际标签；机器路径、启动和备份归 PCConfig。快速增减见用户手册《本地模型统一配置》。普通配置增减以必要检查和读回为准，用户明确不要求重复 E2E，实际故障再定向修复；这取代前文将日常同步机械设为待重验的旧流程。四模型目录已安装，当前桌面进程在下次正常启动刷新，未强制重启。

补充（2026-09-15）：Ollama 标签统一使用 型号-参数量[-变体]:上下文，四个标签与 Provider 同步改名，权重和参数哈希未变。同步器先验证保留模型，随后清理全部集合外标签并回读数量和哈希；不保留无用途的临时别名。客户端展示真实型号，去限制版采用一致称呼。该集合只控制本地模型，Desktop 每次启动仍动态获取上游目录；Wallpaper Engine 原生规则只匹配 llama-server.exe，不增加 OCR/ASR 停壁纸逻辑。

空闲恢复补充：用户要求关闭客户端后壁纸及时恢复，机器层采用 Ollama 原生 OLLAMA_KEEP_ALIVE=30s、单并行/单模型设置，不新建对话锁、后台监控器或 OCR/ASR 停壁纸逻辑；真实请求完成后验证空闲卸载，并复测两个 27B 新标签的吞吐。

2026-09-16 性能故障收敛：同一 12571-token 真实会话渲染文本在 `/v1/responses` 与 raw `/api/generate` 同状态下吞吐相近；冷加载交错 512/128/512/128 证明普通 35B 原先未显式约束的 batch 是可操作的缓解点。统一入口新增 `-OllamaNumBatch` 与可选 `-OllamaProfileId`，默认只预览，Apply 通过原生 copy/create 更新同权重标签，直接读取实际清单/参数层并验证其他层不变，然后同步 Provider 与现有消费者。失败先恢复并核对原 digest，恢复失败保留临时标签；不引入协议网关、请求改写、缓存禁用、缩短上下文或新常驻进程。普通 35B 采用 batch=128；35B 去限制版在 128 下输入处理退化，故显式保留原有效值 512；两款 27B 原有 128 不变。四模型均经同一入口同步和验证，不强制采用同一参数。统一入口同时修复已启用 Desktop 桥缺失 dist 产物时不能完成同步的问题：仅缺失时调用现有 Build。具体性能证据、限制与回滚见《本地模型统一配置》，历史性能结论不替代当前验收。
- 已验收的 GLM 用户可见沟通策略抽取为 `scripts/CodexUserCommunicationPolicy.ps1`，并推广到当前全部受管、非 OpenAI Codex 目录：GLM、云端 Qwen、本地 Qwen 与 DeepSeek。它只约束可见进度和最终答复的表达，不改模型能力、Provider、模型身份、上下文、权限、协议或摘要传输；原生 OpenAI/Codex 继续动态发现，绝不写入这项策略。Qwen 生成器和 DeepSeek 生成器在生成目录时调用同一策略，未来登记的 DeepSeek Codex 目录也必须调用并受单元测试约束。DeepSeek 的官方条目哈希验证先在剥离 AICLI 的压缩、目录加载和沟通策略字段后执行，避免把本机策略当成上游元数据，也不放宽官方模型字段校验。

补充（2026-09-16）：受管模型面向用户的沟通

上述策略以模型目录中的附加指令实现，不新增摘要服务、代理层、定时器或全局 Codex 配置。当前只有 GLM 已经通过实际使用验收；Qwen 与 DeepSeek 的本次推广完成静态目录和生成器验证，实际表达由后续使用观察。

## 21. DeepSeek Flash 桌面接入与升级连续性

2026-09-17 将 DeepSeek 的桌面/CLI 主入口统一为 `codex-deepseek-flash` → 官方自动升级 ID `deepseek-flash`。用户界面只显示“DeepSeek Flash”，不把当前实际版本号固化进菜单；旧 `codex-deepseek`、`deepseek-v4-flash` 与 Claude Code / Open Interpreter 的旧 DeepSeek 内置模板均失败关闭。`codex-deepseek-v4-pro` → `deepseek-v4-pro` 继续保留为独立 CLI-only exact Profile，不进入 Desktop 模型目录，也不能用 Flash 的回执代替验收。

Desktop reasoning 展示以用户已验收稳定的 GLM 行为为对照。GLM-5.3 / GLM-5.3-Flash 的即时 raw reasoning → summary 映射和原生生命周期保持不变。2026-09-17 对当前安装的 Codex Desktop 26.911.7940.0 作只读代码核验：其投影器按最后一个非用户条目的位置计算 reasoning/assistant 的活动状态，并非按 reasoning `item/completed` 判断；分组器在正文成为尾部消息时可能把它提前提升为 `assistantItem`，关闭活动区。旧版本在这套真实投影/分组逻辑的事件回放中重现了提前提升；仅推迟 completion 不改变排序。此前把真实 reasoning 合并到首个展示 ID 的版本又被用户验收为摘要丢失，已经撤销，不把该失败版本作为可用基线。

现行兼容仅对 `codex-deepseek-flash` 生效。真实 reasoning 的 ID、started、公开摘要 delta 与 completed 继续保留，不把多段思考重映射到同一 ID。raw content 按真实 item/part 转为 summary 展示；同一 part 只补一次 `summaryPartAdded`。若完成项的 summary/content 为空，使用本轮该真实 item 已经公开流出的摘要补齐，避免空终态擦掉已显示内容；有非空原生完成内容则保留它，真实 encrypted 元数据不作解密或改写。

正文 item 之后增加独立的空结构 reasoning 连续项（`__aicli_deepseek_keepalive_*`，summary/content 均为空，不携带 encrypted 内容），只为保持活动区连续；它不生成思考文字、不合并真实摘要，也不反馈给上游模型。即使之前 reasoning 尚未发送 completed，也必须按条目位置处理正文切换。后续真实 reasoning 先 started 再关闭连续项；新的正文先建立自身位置和新连续项，再结束旧连续项。只有完成事件而无开始事件的正文，先建立同一真实 ID 的空 started，再建立连续项，最后发送原始非空 completed；带初始正文的 started 同样先建立空位置，随后原样释放初始文本，避免先出现非空最终回答的一帧。正常完成、中断、失败均清理本轮连续状态。成组展示事件在传输层持同一写锁一次写出/刷新，其他响应不能插进其中。正文与工具仍实时传输，Qwen、官方 Provider 和 GLM 不进入 DeepSeek 状态机。

验收记录（2026-09-17）：候选 `b69fb5c56c8c2f23` 已通过 60 项 Router 检查、73 项 Provider/引擎升级/隔离回归、7 项 stdio transport，并通过安装后 EXE 的同项 transport 复测。新增 `tests/DesktopBridge/Invoke-RendererReplay.cjs <bridge.exe> <current-app.asar>` 直接读取当前安装包的投影和分组函数，在真实 bridge stdio 上运行 15 组隔离事件回放，覆盖阶段标记、正文交错、空完成项、completion-only 和初始正文；结果为零提前 final 提升、零摘要丢失、最终正文保留。该测试对旧 `20da6e1e33f5f9f9` 能检出原问题，负对照有效。回放只验证实际投影/分组逻辑，无关 Markdown/制品依赖使用 stub，不冒充 DOM、动画或真实模型肉眼验收；上游私有资源不提交到仓库。测试发现提取契约变化时明确报错，不自动改写或锁定用户的 Codex 版本。

同一候选已通过七文件登记和安装哈希回读，自动重启后确认 Desktop → 新 bridge → 官方引擎进程链；安装器保留 `20da6e1e33f5f9f9` 为可回滚基线。当前状态是代码/安装/进程验证完成，用户对真实 DeepSeek 摘要与闪烁的最终 UI 验收待反馈，不能提前记为彻底修复。

Codex 升级连续性不依赖固定版本目录。Desktop 每次启动先解析当前官方 Codex 引擎，再刷新官方模型目录，并把 AICLI 的 Qwen、GLM、DeepSeek 与本地模型以内容哈希版本的桥接产物叠加；官方模型继续由 Codex 动态维护，不写成静态白名单。若新版桥接无法满足当前 app-server 协议、身份或事件合同，必须失败关闭并保留已安装的可回滚版本，禁止用旧静态配置静默覆盖或冒充官方模型。

同日进一步确认 Store AppX 包内资源与 `%LOCALAPPDATA%\OpenAI\Codex\bin\<16-hex>\codex.exe` 可能独立自更新。解析器因此只接受固定目录形态、普通文件、有效 OpenAI 签名且 `codex-cli` 版本不低于包内资源的官方缓存候选；满足时优先使用该官方自更新缓存，否则才回退到包内资源/AICLI upstream cache。此规则与上述内容哈希桥接共同解决“Codex 升级后入口仍指向旧引擎或错误覆盖”的连续性问题，同时不扩大 Provider、模型或权限边界。

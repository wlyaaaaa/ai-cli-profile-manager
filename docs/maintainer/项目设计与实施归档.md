# AI CLI Profile Manager 项目设计与实施归档

归档版本：`0.1.0` 首次公开版本
归档日期：2026-07-14（UTC+8）
目标平台：Windows 11、PowerShell 7

本文是维护者级产品、架构、决策、端口、更新、手册和实施合同的单一归档。用户操作不以本文为准；用户事实源是：

- 《[AI CLI Profile Manager 使用手册](<../user/AI CLI Profile Manager 使用手册.md>)》
- 《[Codex、Claude Code 与 Open Interpreter CLI 中文手册](<../user/Codex、Claude Code 与 Open Interpreter CLI 中文手册.md>)》

## 1. 来源合并说明

本文合并并取代原有：

- 产品设计与用户旅程。
- Profile、端口、更新和中文手册专项合同。
- 产品决策与动态事实纠正记录。
- 0.1.0 实施计划、测试矩阵和完成定义。

旧路径只保留迁移指针，避免项目规则和历史链接失效。研究输入属于非权威归档，不并入产品事实。

## 2. 产品定义

一句话：让 Windows 用户用一个中文入口，按经过验证的 Profile 启动原生 Codex CLI、Claude Code 或当前官方 Rust Open Interpreter，不再反复手改全局环境变量、配置文件和代理进程。

本产品是启动与运维层，不是新的 AI Agent。聊天、历史、终端交互、权限提示、上下文管理和工具执行继续由原生 CLI 负责。

目标用户：

1. 已使用 Codex CLI、Claude Code 或 Open Interpreter，但需要切换官方订阅、国内 API 和本地模型的 Windows 用户。
2. 会使用 PowerShell，但不希望记忆 Provider、URL、套餐、模型和代理细节的用户。
3. 需要同一套可靠配置跨项目使用，同时不污染官方登录状态的用户。

核心原则：

- 原生优先：不重做 TUI、PTY、会话历史或协议网关。
- 明确选择：用户知道本次引擎、Provider、模型、凭据来源和数据去向。
- 进程隔离：不永久写全局 Provider 变量，不覆盖用户基础配置。
- 证据优先：能打开 CLI 不等于模型或工具可用。
- 安全失败：端口、身份、摘要、秘密和配置冲突不确定时拒绝危险动作。
- 可退出：`native` 和 `eject` 让用户能理解、迁移和脱离本工具。

## 3. 首版范围

### 3.1 引擎与 Provider

| 引擎 | 首版公开路径 |
|------|--------------|
| Codex | 官方 ChatGPT/OpenAI、千问 Responses 按量、千问 Token Plan、本机 Ollama |
| Claude Code | 官方 Claude、DeepSeek、千问按量/Coding Plan/Token Plan、Ollama、ChatGPT 双代理、自定义 Anthropic Messages |
| Open Interpreter | 当前官方 Rust `0.0.21+`：千问 Responses、DeepSeek Chat、本机 Ollama |

Codex 不提供 DeepSeek、纯 Chat Completions 或千问 Coding Plan 直连。`codex-custom-responses` 仅作为隐藏扩展能力，不在首版公开模板中展示。

Open Interpreter 的旧 Python 0.4.x、LiteLLM 旧安装链和旧命令参数不属于支持面。

### 3.2 非目标

- macOS、Linux 和 Windows 11 以外的平台。
- GUI、全屏 TUI、PTY 外壳、内置聊天窗口或会话数据库。
- 修改或汉化上游 CLI 本体。
- 自研协议转换服务。
- 自动在不同 Provider 或双代理之间故障转移。
- 静默升级 Codex、Claude Code、Open Interpreter、Ollama 或账号相关组件。
- 自动放宽上游审批、沙箱和完全访问权限。
- 未经验证就承诺智谱等未来 Provider。

## 4. 产品对象与数据模型

### 4.1 三层对象

1. **Provider 模板**：随版本发布的只读声明，记录引擎、Provider、套餐、地域、transport、端点、模型、认证类型、能力、来源和兼容性。
2. **用户 Profile**：保存模板 ID、用户自定义 ID/名称、地域、模型、项目偏好和秘密引用，不保存秘密值。
3. **启动计划**：每次 `start` 临时生成，包含可执行文件、工作目录、参数、子进程环境、移除变量、配置文件和代理依赖。

### 4.2 Manifest 基本字段

`schemaVersion`、`id`、`displayName`、`engine`、`provider`、`plan`、`region`、`transport`、`endpoint`、`models`、`auth`、`proxyRef`、`capabilities`、`compatibility`、`verification`、`sources`、`deprecation`。

Manifest 是无执行能力的数据，不允许嵌入 PowerShell。URL、Header、模型和 ID 在进入启动计划前验证；非 localhost 明文 HTTP 默认拒绝。

### 4.3 状态闭集

- `通过`：单个检查项成功。
- `可用`：当前版本要求的真实验证均有证据。
- `可用但有限制`：静态就绪、文本通过但工具未证明，或存在明确限制。
- `不可用`：关键依赖、协议、配置或真实测试失败。

验证记录必须绑定 Profile 指纹、引擎、Provider、transport、端点、模型、CLI 路径/版本、测试层和时间。配置或版本变化后旧证据不能无条件继承。

## 5. 锁定命令面

```text
aicli
aicli setup
aicli version

aicli profile list [--available] [--json]
aicli profile show <id> [--json]
aicli profile configure <template-id> [--id <profile-id>]
aicli profile set-default <id>
aicli profile remove <id> [--yes]

aicli start <id> [--project <path>] [-- <native-args...>]
aicli native <id>
aicli eject <id> [--output <path>]

aicli doctor [id] [--json]
aicli test <id> --live [--level text|tool|all] [--yes] [--json]

aicli proxy <ccp|cliproxy> install
aicli proxy ccp login [codex|device]
aicli proxy cliproxy login [codex|claude|device]
aicli proxy <ccp|cliproxy> logout [--purge-local-auth] [--yes]
aicli proxy <ccp|cliproxy> configure [--port <port>|--auto-port]
aicli proxy <ccp|cliproxy> start|stop|status [--json]
aicli proxy <ccp|cliproxy> update-check|update|native

aicli update check [codex|claude|ollama|interpreter|ccp|cliproxy|self] [--json]
aicli update guide [codex|claude|ollama|interpreter|ccp|cliproxy|self]

aicli help [compact|model|effort|permissions|resume|compare|<command>]
aicli uninstall [--purge-user-data] [--yes]
```

规则：

- 无参数入口是短文本选择器，不是全屏界面。
- 未给 `--project` 时使用当前目录，目录必须存在。
- 只有 `--` 后的字符串逐项传给上游。
- 与启动计划冲突的 Provider、Base URL、认证和产品自管配置入口必须拒绝。
- 未知命令、参数和值缺失立即返回用法错误，不猜用户意图。
- 删除型动作默认确认；`--yes` 只授权该命令列出的本地删除。
- `test` 必须显式 `--live`；非交互需 `--yes`。

退出码：`0` 成功、`2` 用法错误、`3` 可用但有限制、`4` 不可用、`5` 内部错误、`6` 取消。

## 6. 文件、秘密与进程合同

### 6.1 默认布局

| 内容 | 位置 |
|------|------|
| 用户 Profile/设置 | `%APPDATA%\AiCliProfileManager` |
| 秘密、代理、状态、缓存、日志 | `%LOCALAPPDATA%\AiCliProfileManager` |
| CurrentUser PowerShell 模块 | 当前用户 `PSModulePath` 下的版本目录 |
| 命令垫片 | `%LOCALAPPDATA%\aicli\bin` |

所有路径通过 Windows Known Folders 或实际 `PSModulePath` 解析，不在公开模板中硬编码用户目录或盘符。

### 6.2 秘密

- API Key 使用 DPAPI CurrentUser；安全存储失败时不得回退明文。
- 秘密不得出现在 Git、命令参数、PowerShell 历史、日志、JSON、异常、`show`、`native`、`eject` 或测试快照。
- 代理 OAuth 由上游代理写入隔离目录，产品不读取 token 正文。
- 删除最后一个秘密引用的用户 Profile 时，可同时删除本工具保存的密文，但必须在动作前说明。

### 6.3 子进程

- 使用 `ProcessStartInfo.ArgumentList`，不拼接字符串执行，不对用户输入 `Invoke-Expression`。
- Provider 环境只进入目标子进程；父终端保持不变。
- 父环境中可能劫持 Provider 的变量先从目标子进程移除，再注入必要值。
- `native` 必须展示脱敏后的实际计划；`eject` 只生成无秘密配方。

## 7. 引擎适配合同

### 7.1 Codex

- 公开第三方 transport 只接受 Responses。
- 官方 Profile 使用真实 `CODEX_HOME` 与官方登录，不生成派生配置。
- 第三方 Profile 在真实 Codex home 下只写 `aicli-` 命名空间的独立 Profile 文件，并记录受管标记和内容 hash。
- 不覆盖基础 `config.toml` 或未知文件；用户修改后的受管文件不误删。
- 当前 Codex Profile 格式为 `$CODEX_HOME/<name>.config.toml`；旧 `[profiles.*]` 语法不作为当前合同。
- Codex 思考强度通过当前构建的 `/reasoning`、`/model` 内的 reasoning effort 或配置字段 `model_reasoning_effort` 调整。
- Codex machine run 以已验收的 npm `codex-cli 0.145.0` app-server JSON-RPC v2 为最低协议基线，由一次性内部桥保持双向协议；`0.146.0-alpha.3.1` 已通过真实兼容验收，后续更新版本默认尝试并接受同一运行时门禁。官方与本地 Ollama Codex machine run 使用 Codex 原生 `read-only` / `workspace-write` 沙箱；第三方 Codex 及其他适用引擎继续受外层 Windows 沙箱约束。原生路径直接从 stdin 接收正文，外层沙箱路径才通过仅授权父进程身份与隔离沙箱身份的随机命名管道传递；两者都不得把正文写入 argv、环境变量、工作区或临时文件。
- 观察层只接受 app-server 同一份完整运行时快照中的 `tokenUsage.last.totalTokens` 与 `modelContextWindow`，以及真实 `contextCompaction` item；不以累计 token、配置上限或字符数估算。公开 `agentMessage` delta 按短语聚合成 `output.delta`；公开进度、粗粒度工具事件和最终结果可见，reasoning 正文不公开。`maxSteps` 采用 `distinct-non-output-thread-item-v2`，只约束不同的非输出行动 item；公开 progress/final message 不消耗行动步骤，但仍受墙钟、输出和事件门约束。0.145 的 `subAgentActivity` 是 completion-only 点事件；其他受支持 item 严格执行 started → completed，并绑定精确 thread/turn，成功轮次必须以 `status=completed` 结束。真实复杂任务证明 `0.145.x` 可能为多个公开进度消息省略 completion；兼容层只把全部早于同轮次后续、已完成且有公开正文 final 的 `agentMessage` 标记为 superseded。更新/未知版本、无后续 final、final 之后的新 orphan 或任何非消息 orphan 不得兼容。事件文件不得保存 prompt、reasoning、命令、工具输出或压缩 history；低于基线、必要字段缺失或结构漂移、未知通知、scope/status/lifecycle 不合法、服务端请求或无法确认的 app-server 进程树清理必须失败关闭。

### 7.2 Claude Code

- 直连模板只接受 Anthropic Messages 兼容路径。
- `claude-custom` 要求 HTTPS 或 localhost HTTP、自定义 Key、主模型和可选小模型。
- ChatGPT 订阅只能通过明确标识的第三方 managed-proxy Profile，不伪装直连 Provider。
- Provider 环境只注入子进程，不自动改写用户/项目 `settings.json`。
- 启动前应计算会影响 Provider 的用户、项目、本地和受管设置层；无法安全隔离时阻止启动并指出文件和字段名。
- 权限与 effort 保留 Claude 原生语义；第三方 Provider 能力不得冒充官方模型等价能力。

### 7.3 Open Interpreter Rust

- 只接受当前官方 Rust CLI `0.0.21+`；检测并拒绝旧 Python 0.4.x。
- 千问与 Ollama 使用 Responses wire，DeepSeek 使用 Chat wire。
- 通过临时 `-c` TOML 覆盖设置 `model_provider`、`model`、`base_url`、`wire_api` 和 `env_key`，不写旧 YAML Profile。
- 云端 Key 只进入 `AICLI_OI_PROVIDER_KEY` 子进程环境，并通过 `shell_environment_policy.exclude` 从 Shell 工具排除。
- 不启用旧版自动执行、命令行明文密钥或任何审批/沙箱绕过参数。
- Live 文本测试使用 `exec`、临时目录、只读沙箱、忽略用户配置/规则和最终消息文件。

## 8. ChatGPT 双代理与端口合同

固定上游：

- `ccp`：`raine/claude-code-proxy`，首选端口 `43197`。
- `cliproxy`：`router-for-me/CLIProxyAPI`，首选端口 `43198`。

### 8.1 供应链

- 任一 Windows artifact 必须匹配产品随版本发布的精确 SHA256，或固定可信发布者签名。
- HTTPS、GitHub 来源、下载后临时算摘要或“上游没有 checksum”不能替代批准证据。
- 解压前枚举条目并拒绝绝对路径、`..`、链接逃逸和清单外可执行文件。
- 首次安装在临时目录完成摘要、安全解压和结构验证后才设置 `current`；启动时完成进程身份、健康响应与 loopback 监听验证后才保存运行状态。`0.1.0` 不执行已安装代理的受管版本切换；后续版本若启用更新，必须先完成运行健康验证并支持失败回滚。

### 8.2 端口

- 只允许 IPv4 loopback `127.0.0.1`；`0.0.0.0`、`::`、局域网和公网监听均失败并停止本次受管进程。
- 候选池为 `43192-43209`，两个代理采用不同轮转起点。
- 分配时持有同用户互斥锁，排除 TCP 动态范围、Windows excluded ranges、活动 listener 和真实 bind 失败。
- 不修改动态端口范围、excluded range、防火墙或 URL ACL，不杀未知占用进程。
- 成功启动并验证新进程持有 listener 后才持久化端口。

### 8.3 身份和生命周期

运行状态记录代理 ID、PID、启动时间、可执行路径、版本目录、配置路径、host、port 和 nonce。停止、更新或卸载前重新核验 PID、启动时间、路径和 listener；身份不匹配不终止。

登录只调用上游交互，不读取 OAuth 正文。`logout` 必须分别报告本地清理和远程撤销；强制删除本地认证目录不等于远程撤销。

## 9. Doctor 与 Live Test 合同

### 9.1 Doctor

按稳定 check ID 检查：平台与 PowerShell、CLI/代理/Ollama 版本、安装来源、登录状态、Manifest/Profile/schema/秘密引用、父环境与有效配置层冲突、套餐/地域/端点/模型、代理身份和 listener、Ollama 服务/模型、验证记录有效性。

Doctor 默认不发模型生成请求。没有安全、零费用认证接口时明确写“未执行”，不得用自制 HTTP ping 冒充目标 CLI 已通过。

### 9.2 Live Test

1. 必须显式 `--live`，先展示 Provider、数据去向和额度风险。
2. 创建随机临时空目录，不读取用户项目。
3. 使用真实目标 CLI 的非交互入口，隔离私人规则、设置、MCP、Hooks、Skills 和普通工具。
4. 文本层要求进程退出码 0 且最终模型正文严格等于 `PONG`。
5. 工具层只暴露不接受任意路径/命令的 nonce 工具；无法隔离时跳过并报告限制。
6. 保存结构化版本和 Profile 指纹，不保存提示、回复、项目文件或秘密。
7. `finally` 清理临时配置、工具进程和目录；异常也要覆盖旧验证记录。

## 10. 安装、更新、卸载和恢复

### 10.1 安装

- CurrentUser PowerShell 模块，不需要管理员权限，不全局放宽 ExecutionPolicy。
- 从 Manifest 读取版本，复制到临时候选目录并导入验证。
- `-Force` 才允许替换已有同版本；替换失败恢复备份。
- 创建用户 PATH 垫片并尝试加入 PowerShell 自动导入块。
- 用户数据独立于模块版本，重装或上游 CLI 更新后仍可重新生成启动计划。

### 10.2 更新

- 先识别实际安装来源，再给同渠道命令。
- 不混用原生安装器、npm、WinGet 等渠道。
- 上游 CLI 只检查和指导，不静默升级。
- Open Interpreter Rust 可使用当前官方 `interpreter update`。
- 受管代理只有批准 artifact 才显式更新并支持回滚。
- 本工具 self update 在有真实 GitHub Release 来源后才启用。

### 10.3 卸载

- 普通卸载停止己方代理、删除模块，保留 Profile、DPAPI 秘密、代理数据和导出物。
- `--purge-user-data` 才删除本工具 AppData/Local 数据。
- 不卸载上游 CLI、官方登录或本地模型。
- 删除代理本地 OAuth 前先执行或引导 logout，并分别展示本地删除与远程撤销状态。
- 用户修改过或身份未知的 Codex 派生文件必须保留并报告。

## 11. 用户旅程

### 11.1 首次使用

1. 说明产品只是启动/运维层。
2. 检测 Windows、PowerShell 和目标 CLI。
3. 选择引擎、Provider、套餐、地域和模型。
4. 显示数据去向，无回显录入必要 Key。
5. 运行零费用 Doctor。
6. 用户自行决定是否执行可能耗额度的 Live Test。
7. 在当前目录启动或仅保存 Profile。

首次引导不能要求用户先理解 TOML、环境变量或代理原理。

### 11.2 日常使用

- 显式 `start <id>` 优先，默认/最近 Profile 只用于减少输入，不隐藏选择。
- 项目目录来自当前 shell 或显式 `--project`。
- 切换 Provider 启动新进程；不会在已有会话中热切换。
- `profile show` 看数据去向，`native` 看启动计划，`doctor` 看静态问题，`test --live` 才证明真实链路。

### 11.3 反废案

- `native` 展示脱敏真实配置和命令。
- `eject` 导出无秘密独立配方。
- 代理型导出记录精确仓库、版本、asset、SHA256、安装和重新登录说明，不复制 OAuth 或捆绑未知二进制。

## 12. 中文手册合同

每个功能先写：

1. 功能干什么。
2. 可复制命令。
3. 执行后会怎样。
4. 何时生效。
5. 1–3 个常用例子。
6. 出错先做什么。
7. 最后才放协议、文件和边界。

风险必须在动作前说明：额度、联网、写入、删除、代理启停、Provider 数据去向、远程撤销与本地删除、新进程生效。

解释中文优先，命令和上游错误保留英文。不要虚构参数，不把“打开成功”写成 Provider 可用，不混淆下一请求、新会话和新进程。

用户文档只维护两本 canonical Markdown；学习篇放在 CLI 手册最后。旧页面仅作迁移指针，不复制正文。PDF 从同版本 Markdown 生成，并单独做视觉验收。

## 13. 实施阶段归档

### 阶段 0：动态事实

核对 Codex/Claude/OI 当前 CLI、Profile 格式、Provider 端点、模型、代理版本与 Windows 行为。

### 阶段 1：模块与路由

实现单一入口、严格命令语法、统一退出码、UTF-8 输出、CurrentUser 安装与开发启动器。

### 阶段 2：Manifest 与 Profile

实现 schema、模板加载、多实例用户 Profile、原子 JSON、状态词汇和 Profile 指纹。

### 阶段 3：秘密与启动计划

实现 DPAPI、无回显输入、进程环境隔离、参数数组、`native`、`eject` 和端点/ID/模型校验。

### 阶段 4–5：Codex 与 Claude

按官方、第三方云 API、本地 Ollama、custom 顺序完成 adapter；验证官方登录不被第三方配置劫持。

### 阶段 6：双代理

实现批准 artifact、安全解压、端口互斥与重试、loopback、进程身份、登录/退出、更新回滚和两个 Claude Profile。

### 阶段 7：Doctor 与 Live Test

实现分层 check、真实 CLI 文本探针、工具隔离、版本/指纹记录和异常清理。

### 阶段 8：帮助、更新、安装和恢复

实现命令帮助、来源检测、原子安装、PATH 垫片、卸载、恢复和 self update 边界。

### 阶段 9：中文手册

完成两本 canonical Markdown、索引、迁移指针、法律和兼容性状态页。

### 阶段 10：公开候选

完成离线测试、打包、SHA256、第三方清单、秘密/路径扫描、干净账户安装/重装/升级/卸载和真实 Provider 矩阵。公开远端、PR 和 Release 仍需要单独授权。

## 14. 自动测试与真实验收

自动测试至少覆盖：

- Router：语法、未知参数、`--`、退出码、纯净 JSON。
- Paths/JSON/Secrets：Known Folders、中文/空格路径、原子写入、DPAPI、脱敏。
- Manifest/Profile：schema、套餐、状态、删除和秘密引用。
- Codex/Claude/OI adapter：配置优先级、保留参数、秘密不进 argv、Rust/旧 Python 识别。
- Child process：环境注入/清理、超时、取消和大输出。
- Proxy/ports：动态和排除范围、IPv4/IPv6 listener、并发、PID 复用、摘要、解压和回滚。
- Doctor/Live：无网络静态检查、严格 PONG、工具跳过、过期证据、临时目录清理。
- Docs/Release：两本手册、索引顺序、本地链接、动态来源、无旧 OI 接口、三套思考命令不混用、安装/卸载和包内容。

真实验收使用有权访问的账号/Key，在临时空目录执行；CI 不保存真实秘密、不跑付费请求。每条路径分别记录文本、工具、版本、模型、端点和限制。

## 15. 决策史

### D-001 至 D-009：产品形态

- D-001：建立独立公开项目与独立品牌 `AI CLI Profile Manager` / `aicli`。
- D-002：保留原生 CLI，不做 GUI、PTY 或会话外壳。
- D-003：Codex 首版只做官方、Ollama 和千问 Responses。
- D-004：Claude 首版覆盖官方、DeepSeek、千问、Ollama、双代理和 Anthropic custom。
- D-005：双代理固定为 `raine/claude-code-proxy` 与 `router-for-me/CLIProxyAPI`。
- D-006：不汉化 CLI 本体；中文手册保留原生命令。Codex 思考入口按当前构建记录为 `/reasoning`，并保留 `/model` 内 reasoning effort 的兼容说明；Claude 保持自己的 effort 命令。
- D-007：Provider Manifest 允许后续扩展，但不预先承诺新厂商。
- D-008：公开状态必须有当前证据。
- D-009：首个版本从 `0.1.0` 开始，稳定后再讨论 `1.0.0`。

### D-010 至 D-018：安全与运维

- D-010：不自动故障转移或静默升级。
- D-011：默认不绕过原生权限。
- D-012：上游更新只检查和指导。
- D-013：代理端口动态验证、成功后持久化。
- D-014：中文手册先讲用途、命令和效果。
- D-015：项目代码使用 MIT License；第三方许可证独立。
- D-016：秘密使用 DPAPI CurrentUser。
- D-017：CurrentUser PowerShell 模块安装，不要求管理员。
- D-018：首版命令面锁定，品牌和版本应集中定义。

### D-019 至 D-027：验证与实施修正

- D-019：Ollama 不设跨硬件万能推荐模型；公共模板只使用标准端口和公开模型。
- D-020：未知代理二进制绝不下载执行。
- D-021：工具自检只暴露单用途 nonce 工具。
- D-022：Codex 复用真实 home，只写可验证身份的派生 Profile。
- D-023：本地 OAuth 清理不冒充远程撤销。
- D-024：无法证明工具隔离时诚实跳过并降为有限制。
- D-025：代理批准清单可以为空；无批准证据时拒绝安装。
- D-026：仓库身份、机器配置与产品运行态由各自事实源管理，不能把机器私有事实写进公开产品文档。
- D-027：统一 JSON 外壳、开发入口、代理型 eject 锁文件、离线 Smoke/CI 和兼容性单页作为实施增强。

### D-028：Open Interpreter 世代切换

首版只支持当前官方 Rust Open Interpreter `0.0.21+`，拒绝旧 Python 0.4.x。Provider 采用 Rust CLI 的 TOML 配置与环境密钥引用，不能把旧参数兼容层继续作为公开能力。

## 16. 动态事实纠正记录

- Codex 当前 Profile 是独立 `<name>.config.toml` 文件，不使用旧 `[profiles.*]` 嵌套方案。
- Codex 第三方 Provider 只接受 Responses wire；Chat Completions 不能只改 Base URL 强行接入。
- 千问按量、Token Plan 和 Coding Plan 的 Key、端点与能力必须分开。
- 双代理是第三方本地程序，不是 OpenAI/Anthropic 官方能力。
- Codex reasoning effort 通过当前构建的 `/reasoning`、`/model` 内选择或配置字段调整。
- Open Interpreter 当前官方实现为 Rust；旧 Python 文档、参数和本机验证记录全部失效，必须重新验收。
- 讨论归档或截断输入不能直接成为实现真相。

## 17. 官方与上游参考

### Codex / OpenAI

- [Profiles](https://learn.chatgpt.com/docs/config-file/config-advanced#profiles)
- [Custom model providers](https://learn.chatgpt.com/docs/config-file/config-advanced#custom-model-providers)
- [Configuration schema](https://learn.chatgpt.com/docs/config-schema.json)
- [Slash commands](https://learn.chatgpt.com/docs/developer-commands)

### Claude Code / Anthropic

- [Commands](https://code.claude.com/docs/en/commands)
- [Environment variables](https://code.claude.com/docs/en/env-vars)
- [Model and effort](https://code.claude.com/docs/en/model-config)
- [Permissions](https://code.claude.com/docs/en/permissions)
- [Installation](https://code.claude.com/docs/en/installation)

### Open Interpreter

- [Repository](https://github.com/openinterpreter/openinterpreter)
- [Install](https://www.openinterpreter.com/docs/terminal/install)
- [CLI Reference](https://www.openinterpreter.com/docs/terminal/cli-reference)
- [Config](https://www.openinterpreter.com/docs/terminal/config)
- [Providers](https://www.openinterpreter.com/docs/terminal/providers)

### Provider 与代理

- [千问接入 Codex](https://help.aliyun.com/zh/model-studio/codex)
- [千问接入 Claude Code](https://help.aliyun.com/zh/model-studio/claude-code)
- [DeepSeek Claude Code integration](https://api-docs.deepseek.com/guides/agent_integrations/claude_code)
- [Ollama Codex](https://docs.ollama.com/integrations/codex)
- [Ollama Claude Code](https://docs.ollama.com/integrations/claude-code)
- [raine/claude-code-proxy](https://github.com/raine/claude-code-proxy)
- [router-for-me/CLIProxyAPI](https://github.com/router-for-me/CLIProxyAPI)

## 18. 公开发布完成定义

发布前必须同时满足：

1. 干净 Windows 11 标准用户可安装、重装、升级、卸载和恢复。
2. 所有公开命令、未知参数、中文/空格路径和退出码自动测试通过。
3. 两本 canonical 手册与实现一致；旧页面仅作迁移入口。
4. 三套思考命令不混用，OI 文档无旧 Python 安装步骤或旧接口。
5. Release ZIP 不含机器私有路径、研究输入、原始日志、秘密或维护者规则。
6. ZIP、SHA256、SBOM/第三方版本清单和许可证齐全。
7. 当前 Provider 矩阵有真实目标 CLI 的版本、模型、端点、指纹和结果；未测路径明确标待验收。
8. 双代理仍为可选第三方通道；未完成 OAuth/Live 时不标可用。
9. GitHub Security 私密报告入口、隐私、贡献、第三方声明和 Changelog 可用。
10. 公开远端、push、PR 和 Release 只在获得明确授权后执行。
